var inserir = 1
var notas = [1, 2, 5, 7, 8, 9, 3, 5, 3, 9, 10]
var numeroInt
var contador
   
    
    numeroInt = prompt("Insira um número inteiro e postivo: ")
    for(let i = 0; i <= notas.length; i++){
        if(numeroInt == notas[i]){
            console.log(i)
        }
    }
